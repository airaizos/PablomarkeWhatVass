//
//  ChatWireframe.swift
//  WhatsVass
//
//  Created by Pablo Marquez Marin on 11/3/24.
//

import UIKit
import SwiftUI
/*
final class ChatWireframe {

    // MARK: - Properties
    private var chat: Chat

    init(chat: Chat) {
        self.chat = chat
    }

    // MARK: - Public methods
    func push(navigation: UINavigationController?) {
        let viewController = self.viewController
        navigation?.pushViewController(viewController, animated: true)
    }

    // MARK: - Private methods
    private var viewController: UIViewController {
        let apiClient = ChatAPIClient()
        let dataManager = createDataManager(apiClient: apiClient)
        let viewModel = ChatViewModel(dataManager: dataManager, chat: self.chat)
        return ChatViewController(viewModel: viewModel)
    }

    private func createDataManager(apiClient: ChatAPIClient) -> ChatDataManagerProtocol {
        return ChatDataManager(apiClient: apiClient)
    }
}
*/
